import re

for _ in range(int(input())):
    card = input().strip()

    if not re.match(r'^[456]\d{3}(-?\d{4}){3}$', card):
        print("Invalid")
    elif re.search(r'(\d)\1\1\1', card.replace('-', '')):
        print("Invalid")
    else:
        print("Valid")
